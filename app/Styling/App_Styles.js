

const SubHeading = {
    color: '#AAA',
    textAlign: 'center',
    fontSize: 10,
    fontWeight: 'bold',
}

const CenteredObject = {
    textAlign: 'center',
    fontWeight: 'bold',
}

const GreyBorder = {
    borderBottomWidth: 1,
    borderColor: '#CCC'
}

const PrimaryFont = 'SFRound'

// * POLLISH COLOR SET
const PrimaryPollish = '#58e4bc'

const NavColor = '#1F71EB'

const OptionColors = ['#EF946C','#5ED1D0','#DC6BAD', '#000'] 
const FadedOptionColors = ['#FBDFD3', 'rgba(151, 216, 196, 0.3)', 'rgba(220, 107, 173, 0.3)', 'rgba(0, 0, 0, 0.3)']
const VeryFadedOptionColors = ['rgba(239, 148, 108, 0.03)', 'rgba(151, 216, 196, 0.03)', 'rgba(220, 107, 173, 0.03)', 'rgba(0, 0, 0, 0.03)']

export {SubHeading, CenteredObject, PrimaryPollish, GreyBorder, OptionColors, FadedOptionColors, NavColor, 
        PrimaryFont, VeryFadedOptionColors}